Act as a Senior Creative Developer. We need to rebuild the "Deployment Mechanics" section into a scroll-driven timeline animation (Scroll-bound reveal).

**The Concept ("The Deployment Thread"):**
There is a vertical timeline tracking the 3 steps. As the user scrolls down the section, a 1px black line grows downwards dynamically tied to the scroll position. When the black line reaches a step, that step transitions from muted/dim to fully active (black). If they scroll up, it reverses perfectly.

DELETE the current HTML/CSS for this section and REPLACE it with this exact logic.

**1. Required HTML Structure:**
<section class="deployment-section" id="deployment">
  <h2 class="section-title">Deployment Mechanics</h2>
  <div class="timeline-container">
    <div class="timeline-track">
      <div class="timeline-fill" id="timeline-fill"></div>
    </div>
    
    <div class="timeline-step" data-step="1">
      <div class="step-num">01 /</div>
      <div class="step-info">
        <h3>Tap Physical Object</h3>
        <p>Card / Figure / Sticker</p>
      </div>
    </div>
    
    <div class="timeline-step" data-step="2">
      <div class="step-num">02 /</div>
      <div class="step-info">
        <h3>Learn & Earn</h3>
        <p>Gamified modules → Earn Sats</p>
      </div>
    </div>
    
    <div class="timeline-step" data-step="3">
      <div class="step-num">03 /</div>
      <div class="step-info">
        <h3>Peer-to-Peer Growth</h3>
        <p>Share cards → Network expands</p>
      </div>
    </div>
  </div>
  
  <div class="cost-box">Cost of onboarding: $1.50 per user</div>
</section>

**2. CSS Requirements (Pure Minimalist):**
- `.timeline-container`: `position: relative`, `display: flex`, `flex-direction: column`, `gap: 4rem`, `padding-left: 3rem`, `margin: 5rem 0`.
- `.timeline-track`: `position: absolute`, `left: 0`, `top: 0`, `bottom: 0`, `width: 1px`, `background: #E5E5E5`.
- `.timeline-fill`: `position: absolute`, `left: 0`, `top: 0`, `width: 100%`, `height: 0%`, `background: #000000`. (Height will be controlled by JS).
- `.timeline-step`: `display: flex`, `gap: 2rem`, `opacity: 0.2`, `transform: translateX(-10px)`, `transition: all 0.6s cubic-bezier(0.2, 0.8, 0.2, 1)`.
- `.timeline-step.is-active`: `opacity: 1`, `transform: translateX(0)`.
- Typography: `h3` in Inter 500, `p` in Inter 400 (gray), `.step-num` in JetBrains Mono (large, e.g., 2rem).

**3. JavaScript Logic (Scroll bound):**
Write a highly optimized JS script using `window.addEventListener('scroll')` and `requestAnimationFrame`.
- Calculate the bounding rect of `#deployment`.
- Calculate the scroll progress percentage (0 to 100) specifically inside this container.
- Apply this percentage to `#timeline-fill`'s height (e.g., `element.style.height = progress + '%'`).
- Determine thresholds (e.g., 20%, 50%, 80%). When the progress passes a threshold, add `.is-active` to the corresponding `.timeline-step`. Remove it if scrolling back up.

Output the complete HTML, CSS, and JS.